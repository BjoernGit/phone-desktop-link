{
  "name": "phone-desktop-link",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "npm --prefix client ci && npm --prefix client run build && npm --prefix server ci",
    "start": "npm --prefix server start"
  }
}
